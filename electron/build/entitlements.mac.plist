<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Electron needs both of these under the hardened runtime or the app
       will not launch at all. This is not optional hardening to trade away. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>

  <!-- Voice channels. Without these the OS denies capture silently under a
       signed, hardened build, which looks exactly like a broken app. The
       matching NSMicrophoneUsageDescription / NSCameraUsageDescription live
       in build.mac.extendInfo. -->
  <key>com.apple.security.device.audio-input</key>
  <true/>
  <key>com.apple.security.device.camera</key>
  <true/>
</dict>
</plist>
